# Core dependencies
pandas
streamlit
requests
aiohttp
Pillow

# For type hints
typing-extensions

# Project-specific (agents)
openai

# Scraping and config dependencies
playwright
pyyaml
python-dotenv
beautifulsoup4
lxml

# If you use re, json, os, sys, pathlib, hashlib, asyncio, they are all standard library and do not need to be listed.
